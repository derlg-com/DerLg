The complete Design PRD has been written to:

**`/home/rayu/derlg-v2/docs/design.md`**

It contains the following sections:

1. **Design Philosophy & Principles** — five principles (tropical calm, chat-as-engine, trust, mobile-first PWA, informational motion).
2. **Color System** — primitive palette, semantic CSS variables for light and dark modes, contrast notes.
3. **Typography** — font families (Playfair Display, Geist, Noto Sans Khmer, JetBrains Mono), full type scale with weights/line-heights/letter-spacing, Khmer/Chinese considerations.
4. **Spacing & Layout** — 4px spacing scale, layout tokens, breakpoints, safe-area insets.
5. **Component Library** — shadcn/ui base components to install, plus ten custom components (BrandLogo, BottomNav, SplitScreenLayout, ChatPanel, ContentStage, ContentItemShell, QuickReplyChips, CountdownTimer, ConnectionPill, catalog cards) with props, behavior, and accessibility notes.
6. **Page Designs** — layout, sections, responsive behavior, and interactions for `/`, `/login`, `/register`, `/vibe-booking`, and `/profile`.
7. **Vibe Booking Split-Screen Detail** — full ChatPanel anatomy, ContentStage anatomy, fifteen content renderers, mobile behavior, and empty/loading/error states.
8. **Animations & Micro-interactions** — exact durations, easings, and transforms for chat auto-scroll, content enter/exit, button feedback, skeleton shimmer, countdown pulse, panel collapse/resize, and booking confirmation.
9. **Iconography & Imagery** — Lucide/Phosphor icon set, image aspect ratios, map tile style, placeholder rules.
10. **Accessibility (WCAG AA)** — contrast, focus states, ARIA roles, reduced-motion gating, touch targets, screen-reader copy.
11. **PWA / Mobile-First Notes** — safe areas, bottom nav, install prompt, offline fallback, viewport/status bar.
12. **Asset Requirements** — table of required images/icons/illustrations with dimensions, formats, and sourcing guidance.

The document also includes a **Design Token Quick Reference** and a **Change Log**.
