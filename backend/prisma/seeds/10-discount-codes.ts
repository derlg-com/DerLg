// =============================================================================
// Seed: 10 — Discount codes
// =============================================================================

import type { PrismaClient } from '@prisma/client';

export = async function seed(prisma: PrismaClient): Promise<void> {
  console.log('  • discount_codes — no default data yet, skipping');
}